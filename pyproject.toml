[project]
name = "privatesearch"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115", "uvicorn>=0.30", "beautifulsoup4>=4.12"]
[project.optional-dependencies]
test = ["pytest>=8"]
[project.scripts]
privatesearch = "privatesearch.cli:main"
